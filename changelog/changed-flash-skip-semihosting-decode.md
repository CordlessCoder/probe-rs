A flash algorithm's return is no longer decoded as a possible semihosting call. The algorithm halts
on the breakpoint the debugger placed at its own return address, so the program counter read and
the instruction read that decided it were spent confirming a known answer once per sector erased
and page programmed.
