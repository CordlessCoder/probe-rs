Cortex-M: resuming no longer single-steps first when the host has just written the program counter. The step exists so a breakpoint on the current instruction cannot halt the core again immediately, which cannot happen once the program counter has been moved. It is expensive — it disables every breakpoint, steps, decodes the halt and restores them — and flashing pays it on every flash algorithm call.
