While a flash algorithm is running, the core's start and stop are reported to the probe once for
the whole operation rather than once per sector erased and page programmed. Probes that drive an
indicator from it still show the target running; the round trip it cost on every cycle is gone.
